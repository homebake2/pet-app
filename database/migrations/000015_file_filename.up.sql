-- Исходное имя файла, как выбрано пользователем при загрузке (см. «Общие
-- требования: Файлы сущностей», раздел «Модель данных»). Nullable: не все
-- owner_type используют это поле (например, pet_photo).
ALTER TABLE file ADD COLUMN filename text NULL;
